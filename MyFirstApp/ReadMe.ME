Hubsystem-UI
==========

Introduction
------------
Hubsystem-UI is the front-end service to manage hubs.

Build Instruction
-----------------
Pre-requisties:
1) Java 1.7
2) Maven

Build process:
1. To build the jar file: mvn clean install
2. To run in local environment: java -jar target/samasara.app-0.0.1.jar server src/main/resources/configuration/prod.yml
   use hubsystem-ui-stage.yml for stage
3. Open localhost:27030/

